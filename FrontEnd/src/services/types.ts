/**
 * Type definitions for API requests and responses
 * These should match your backend DTOs
 */

// Common Types
export enum Role {
  Admin = 0,
  Journalist = 1,
  Organization = 2,
  RegularUser = 3,
}
